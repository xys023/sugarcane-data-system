# 甘蔗田间数据收集系统 - 部署教程

> 本教程面向零基础用户，按照步骤操作即可完成部署。如有疑问，请参考文末「常见问题」。

---

## 📋 系统简介

本系统包含三部分：
1. **后端服务**（Node.js + SQLite）：数据存储和API接口，需要在电脑/服务器上运行
2. **PC端后台**（网页）：数据管理、查看、导出，通过浏览器访问
3. **微信小程序**（移动端）：田间数据采集，通过微信开发者工具打开

---

## 🖥️ 第一部分：PC端后台 + 后端服务部署

### 步骤1：安装 Node.js（必需）

Node.js 是运行后端服务的基础环境。

1. 打开浏览器，访问：https://nodejs.org/zh-cn/
2. 点击「LTS」版本下载（长期支持版，**必须22.x或更高版本**）
3. 下载完成后，双击安装包
4. 安装过程中**全部点「下一步」**即可，不需要修改任何设置
5. 安装完成后，验证是否成功：
   - 按 `Win + R`，输入 `cmd`，回车
   - 在黑色窗口中输入 `node --version`，回车
   - 如果显示版本号（如 `v22.0.0` 或更高），说明安装成功

### 步骤2：解压项目文件

1. 将项目压缩包 `sugarcane-data-system.zip` 解压到一个固定位置
   - 建议解压到 `D:\sugarcane-data-system` 或其他非中文路径
   - **注意：路径中不要包含中文和空格**

2. 解压后，目录结构应该是：
   ```
   sugarcane-data-system/
   ├── server/          # 后端服务
   ├── admin/           # PC端后台网页
   ├── miniprogram/     # 微信小程序源码
   ├── deploy/          # 启动脚本
   └── 部署教程.md       # 本文件
   ```

### 步骤3：安装项目依赖（首次运行必需）

1. 打开项目文件夹，进入 `deploy` 目录
2. **双击运行「安装依赖.bat」**
3. 等待安装完成（首次需要3-5分钟，取决于网络速度）
4. 看到「依赖安装完成」提示后，按任意键关闭窗口

> 如果安装失败，请检查网络连接，或手动在 `server` 目录下打开命令行，执行 `npm install`

### 步骤4：启动服务

1. 在 `deploy` 目录下，**双击运行「启动服务.bat」**
2. 窗口中会显示：
   ```
   ========================================
     甘蔗田间数据收集系统 - 后端服务已启动
   ========================================
     服务地址: http://localhost:3000
     PC端后台: http://localhost:3000/
     默认管理员账号: admin / admin123
   ========================================
   ```
3. **不要关闭这个黑色窗口**，关闭窗口服务就会停止

### 步骤5：访问PC端后台

1. 打开浏览器（推荐 Chrome 或 Edge）
2. 在地址栏输入：`http://localhost:3000`，回车
3. 进入登录页面，使用默认账号登录：
   - 用户名：`admin`
   - 密码：`admin123`
4. 登录成功后即可使用PC端后台的所有功能

> **重要：首次登录后请立即修改默认密码！** 在「个人中心」或「用户管理」中修改。

---

## 📱 第二部分：微信小程序部署

### 步骤1：安装微信开发者工具

1. 访问：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. 下载「稳定版」的 Windows 64位 版本
3. 双击安装，全部默认下一步即可

### 步骤2：注册小程序账号（如果还没有）

1. 访问：https://mp.weixin.qq.com/
2. 点击「立即注册」，选择「小程序」
3. 按照提示完成注册（需要邮箱、身份证等信息）
4. 注册成功后，在「开发」-「开发管理」-「开发设置」中找到 **AppID**

> 如果只是本地测试，可以使用「测试号」，不需要注册。在微信开发者工具中选择「测试号」即可。

### 步骤3：导入小程序项目

1. 打开微信开发者工具
2. 点击「+」号或「导入项目」
3. 填写项目信息：
   - **项目名称**：甘蔗田间数据收集
   - **目录**：选择项目中的 `miniprogram` 文件夹
   - **AppID**：填入你自己的小程序AppID，或选择「测试号」
4. 点击「导入」

### 步骤4：配置后端服务地址

小程序需要知道后端服务在哪里，需要修改配置：

1. 在微信开发者工具中，打开 `app.js` 文件
2. 修改以下两行（约第10-11行）：
   ```javascript
   apiBase: 'http://localhost:3000/api',
   uploadUrl: 'http://localhost:3000/api/upload/image',
   ```
   - **本地测试**：保持 `http://localhost:3000` 不变
   - **局域网使用**：改为电脑的局域网IP，如 `http://192.168.1.100:3000`
   - **服务器部署**：改为你的服务器域名或IP，如 `http://你的服务器IP:3000`

3. 按 `Ctrl + S` 保存

### 步骤5：开启不校验合法域名（开发阶段必需）

1. 在微信开发者工具右上角，点击「详情」
2. 选择「本地设置」标签
3. 勾选「不校验合法域名、web-view（业务域名）、TLS版本以及HTTPS证书」
4. 关闭详情窗口

> 这是因为开发阶段使用的是 `http://localhost`，不是正式的HTTPS域名。正式上线时需要配置HTTPS域名。

### 步骤6：预览小程序

1. 在微信开发者工具中，点击顶部的「预览」按钮
2. 用手机微信扫描弹出的二维码
3. 即可在手机上体验小程序
4. 使用之前在PC端后台创建的账号登录即可

---

## 🌐 第三部分：局域网/服务器部署（多人使用时需要）

如果希望多人（如农科院多个研究员）同时使用，需要把服务部署到局域网或公网服务器上。

### 方案A：局域网部署（适合办公室内使用）

1. 确保运行服务的电脑和手机连接**同一个WiFi**
2. 查看电脑的局域网IP：
   - 按 `Win + R`，输入 `cmd`，回车
   - 输入 `ipconfig`，回车
   - 找到「IPv4地址」，如 `192.168.1.100`
3. 修改小程序 `app.js` 中的地址为 `http://192.168.1.100:3000/api`
4. 其他人在浏览器访问 `http://192.168.1.100:3000` 即可使用PC端后台
5. 手机扫描预览二维码即可使用小程序

### 方案B：云服务器部署（适合随时随地使用）

1. 购买一台云服务器（推荐阿里云、腾讯云，配置2核4G以上即可）
2. 服务器操作系统选择 **Windows Server**（对小白更友好）或 **Ubuntu**
3. 在服务器上安装 Node.js（同第一部分步骤1）
4. 将项目文件上传到服务器（可以用远程桌面复制，或用FTP工具）
5. 在服务器上运行「安装依赖.bat」和「启动服务.bat」
6. 在服务器安全组中放行 **3000端口**
7. 用服务器公网IP访问：`http://你的服务器IP:3000`
8. 修改小程序 `app.js` 中的地址为服务器地址

> **注意**：正式上线建议配置域名和HTTPS证书，小程序正式版要求HTTPS。

---

## 📊 功能使用指南

### PC端后台功能

| 功能模块 | 说明 |
|---------|------|
| 数据概览 | 查看系统统计数据、最近记录、病害统计 |
| 试验田管理 | 新增、编辑、删除试验田，查看详情 |
| 植株管理 | 新增（支持批量）、编辑、删除植株档案 |
| 生长记录 | 查看、搜索、删除生长记录数据 |
| 病害记录 | 查看、搜索、删除病害记录数据 |
| 数据导出 | 按时间周期导出生长记录、病害记录、综合报表为Excel |
| 用户管理 | （管理员）新增用户、重置密码、启用/禁用用户 |

### 小程序功能

| 功能模块 | 说明 |
|---------|------|
| 首页 | 功能入口、统计数据、最近记录 |
| 生长数据录入 | 选择植株，录入株高、茎径、叶片数等，**自动验证数据合理性** |
| 病害记录 | 记录病害类型、严重程度、症状、处理措施，支持拍照 |
| 试验田管理 | 查看试验田列表和详情 |
| 植株管理 | 查看植株列表和生长历史 |
| 我的记录 | 按时间筛选查看生长记录和病害记录 |
| 数据导出 | 选择导出范围，生成Excel（在PC端下载） |
| 个人中心 | 修改密码、关于系统、退出登录 |

### 数据验证规则（核心功能）

系统会自动验证录入的生长数据是否合理：

1. **株高验证**：新录入的株高如果比上次测量矮超过5cm，会提示错误
2. **茎径验证**：茎径如果比上次明显减小（超过20%），会提醒确认
3. **范围验证**：株高(0-600cm)、茎径(5-80mm)、叶片数(0-50)等超出合理范围会提示
4. **日期验证**：同一植株同一天不能重复录入
5. **趋势验证**：如果本次数据高于后续记录，会提醒注意数据一致性

> 验证不通过时，用户可以选择「确认保存（跳过验证）」来强制保存，但建议先核实数据。

---

## ❓ 常见问题

### Q1：双击「启动服务.bat」后窗口一闪而过？
A：说明Node.js没有正确安装。请重新安装Node.js，安装时勾选「Add to PATH」选项。

### Q2：浏览器访问 http://localhost:3000 显示无法访问？
A：请确认「启动服务.bat」的黑色窗口是否打开着，窗口中是否显示「服务已启动」。如果窗口报错，请截图查看错误信息。

### Q3：小程序预览后显示「网络请求失败」？
A：请检查：
1. 后端服务是否已启动
2. `app.js` 中的地址是否正确
3. 微信开发者工具中是否勾选了「不校验合法域名」
4. 手机和电脑是否在同一网络（局域网部署时）

### Q4：如何修改管理员密码？
A：登录PC端后台后，在「用户管理」中找到admin用户，点击「重置密码」。或在数据库中直接修改（不推荐）。

### Q5：数据存在哪里？会丢失吗？
A：数据存储在 `server/sugarcane.db` 文件中（SQLite数据库）。只要不删除这个文件，数据就不会丢失。建议定期备份这个文件。

### Q6：如何备份数据？
A：直接复制 `server/sugarcane.db` 文件到其他位置即可。恢复时把备份的文件覆盖回去。

### Q7：端口3000被占用了怎么办？
A：可以修改端口。编辑 `server/server.js`，找到 `const PORT = process.env.PORT || 3000;`，把3000改成其他端口（如8080），然后重启服务。访问地址也要改成对应端口。

### Q8：小程序可以正式上线发布吗？
A：可以。需要：
1. 注册正式的小程序账号并完成认证
2. 后端服务部署到有HTTPS证书的域名上
3. 在小程序后台配置「request合法域名」和「uploadFile合法域名」
4. 在微信开发者工具中点击「上传」，然后在小程序后台提交审核

---

## 📞 技术支持

如遇到本教程未覆盖的问题，请检查：
1. 服务启动窗口是否有报错信息
2. 浏览器控制台（按F12）是否有报错
3. 微信开发者工具控制台是否有报错

---

## 📝 更新日志

- v1.0.0（2026-08-21）：首个版本发布
  - 后端API服务 + SQLite数据库
  - PC端后台管理系统
  - 微信小程序端
  - 生长数据实时验证
  - Excel数据导出功能
